{
  "name": "@chatovo/tauri",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "predev": "node ./scripts/kill-stale.mjs",
    "dev": "tauri dev",
    "build": "tauri build",
    "build:debug": "tauri build --debug",
    "android:init": "bun --env-file .env tauri android init",
    "android:dev": "bun --env-file .env tauri android dev",
    "android:build": "bun --env-file .env tauri android build",
    "android:build:aab": "bun --env-file .env tauri android build -- --aab",
    "info": "tauri info",
    "icon": "tauri icon",
    "signer:sign": "tauri signer sign",
    "signer:generate": "tauri signer generate -w ~/.tauri/chatovo.key --password \"\""
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
