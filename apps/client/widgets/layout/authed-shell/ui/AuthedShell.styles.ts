export const authedShellStyles = {
  root: 'flex h-full w-full flex-col overflow-hidden md:flex-row',
  desktopOnly: 'hidden h-full md:flex',
  content: 'min-h-0 min-w-0 flex-1 overflow-hidden bg-background',
} as const;
